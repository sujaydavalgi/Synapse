# spanda-hardware

Builtin robot hardware profile catalog shared by `spanda-core` verify and `spanda-package` validation.

Extracted to break the `spanda-package` → `spanda-core` dependency cycle during the Phase 4 compiler split.
