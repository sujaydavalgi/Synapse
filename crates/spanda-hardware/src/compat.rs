//! Hardware compatibility report item types shared by verify pipelines.
//!
//! Types are now defined in `spanda-connectivity::hardware_types`; re-exported here for
//! backwards compatibility.

pub use spanda_connectivity::{CompatItem, CompatSeverity};
