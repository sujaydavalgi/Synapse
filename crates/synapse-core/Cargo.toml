[package]
name = "synapse-core"
version = "0.1.0"
edition = "2021"
description = "Synapse robotics language — lexer, parser, type checker, and interpreter"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
glob = "0.3"
