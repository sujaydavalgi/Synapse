# spanda-ota

OTA deployment agents, signed artifact bundles, remote rollout, and rollout state for Spanda.

Extracted from `spanda-core` for the lean-core architecture. AST-based deploy plan extraction remains in core until the compiler split.

Used by the `spanda-ota` official package.
