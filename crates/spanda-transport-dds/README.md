# spanda-transport-dds

DDS transport backend for Spanda. Extracted from `spanda-core` for the lean-core architecture.

Enable live UDP multicast with the `live` feature (`SPANDA_LIVE_DDS=1` at runtime).

Used by the `spanda-dds` official package.
