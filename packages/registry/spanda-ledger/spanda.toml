[package]
name = "spanda-ledger"
version = "0.1.0"
description = "Audit ledger anchoring"
license = "Apache-2.0"

[safety]
level = "ledger"

[capabilities]
required = [
  "audit.append",
]

[hardware]
requirements = []
