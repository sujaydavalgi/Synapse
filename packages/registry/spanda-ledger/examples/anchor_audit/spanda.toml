[package]
name = "ledger-anchor-audit"
version = "0.1.0"
description = "Anchor audit records via spanda-ledger community scaffold"
license = "Apache-2.0"

[dependencies]
spanda-ledger = { path = "../../" }

[capabilities]
required = ["audit.append"]

[safety]
level = "experimental"
